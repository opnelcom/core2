module.exports=async ctx=>{const a=ctx.auth();if(!a)return ctx.send(401,{error:'Authentication required'});const tenant=ctx.cookies.current_tenant;if(!tenant)return ctx.send(400,{error:'No current tenant'});const access=await ctx.broker('core_saas','query',{text:`SELECT tenant_user_type FROM core_tenant_user WHERE tenant_id=$1 AND lower(email)=lower($2) AND status='active'`,values:[tenant,a.email]});if(!access.rowCount)return ctx.send(403,{error:'No access'});const r=await ctx.broker('core_saas','query',{text:`SELECT tenant_user_id,email,tenant_user_type,status,added_at FROM core_tenant_user WHERE tenant_id=$1 ORDER BY email`,values:[tenant]});return {users:r.rows};};
